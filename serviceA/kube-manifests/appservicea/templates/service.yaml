apiVersion: v1
kind: Service
metadata:
  name: {{ include "appservicea.fullname" . }}
  labels:
    {{- include "appservicea.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appservicea.selectorLabels" . | nindent 4 }}
